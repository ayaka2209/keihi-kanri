# このファイルを .env という名前でコピーして使います。
#   cp .env.example .env
#
# DATABASE_URL は「どのDBに、誰で、どう繋ぐか」を表す接続文字列です。
#   postgresql+psycopg2://ユーザー:パスワード@ホスト:ポート/DB名
# docker-compose.yml の設定と一致させています。
DATABASE_URL=postgresql+psycopg2://keihi:keihi_dev_password@localhost:5432/keihi
